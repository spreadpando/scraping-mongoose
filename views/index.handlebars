<h1>BOULDER🥩BEEF</h1>

<h2>comments are 100% unmoderated</h2>

<ul>
  {{#each data}}
  <li>
    <a class="open-article" href="/articles/{{this.id}}">
      <p class="post">{{this.headline}}</p>
      <small>{{this.comment.length}} comments</small>
    </a>
  </li>
  {{/each}}
</ul>